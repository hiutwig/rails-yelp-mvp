<div class="container">
  <%= simple_form_for [@restaurant, @review] do |form| %>
  <%= form.input :content %>
  <%= form.input :rating %>
  <%= form.submit "add a review", class: "btn btn-primary" %>
<% end %>
</div>
